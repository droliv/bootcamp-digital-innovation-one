// split quebra a string por um delimitador e retorna um array com os valores
const splitedText = 'Texto'.split('x');

// raplace substitui um valor por outro
const replacedText = 'Texto'.replace('Text', 'text');

// slice retorna a fatia de um valor
const lastChar = 'Texto'.slice(-1);
const withoutLastChar = 'Texto'.slice(0, -1);
const secondToEnd = 'Texto'.slice(1);

// substr retorna N caracteres a partir de uma posição
const twoCharsBeforeFirstPos = 'Texto'.substr(0, 2);



